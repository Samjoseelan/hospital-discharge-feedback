import {X} from "lucide-react";import type {ReactNode} from "react";
export function Modal({title,children,onClose,wide=false}:{title:string;children:ReactNode;onClose:()=>void;wide?:boolean}){return <div className="modalBackdrop" onMouseDown={onClose} role="presentation"><div className={`modal ${wide?"modalWide":""}`} onMouseDown={e=>e.stopPropagation()} role="dialog" aria-modal="true" aria-label={title}><div className="modalHead"><div><span>PATIENT EXPERIENCE</span><h2>{title}</h2></div><button onClick={onClose} aria-label="Close modal"><X size={19}/></button></div>{children}</div></div>}
